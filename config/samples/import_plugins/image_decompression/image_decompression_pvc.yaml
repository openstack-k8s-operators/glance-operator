apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image_import_staging_workspace
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-storage
