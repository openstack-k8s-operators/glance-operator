apiVersion: glance.openstack.org/v1beta1
kind: Glance
metadata:
  name: glance
spec:
  serviceUser: glance
  customServiceConfig: |
    [DEFAULT]
    debug = true
  databaseInstance: openstack
  databaseUser: glance
  keystoneBackend: default
  glanceAPIs:
    default:
      debug:
        service: false
      preserveJobs: false
      replicas: 1
  secret: osp-secret
  storageClass: ""
  storageRequest: 10G
